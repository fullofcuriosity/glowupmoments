{
  "framework": null,
  "buildCommand": null,
  "devCommand": "npx serve public -l $PORT",
  "outputDirectory": "public",
  "routes": [
    { "src": "^/api/(.*)$", "dest": "/api/$1" },
    { "src": "^/builder/?$", "dest": "/api/builder" },
    { "src": "^/(.*)$", "dest": "/$1" }
  ]
}
